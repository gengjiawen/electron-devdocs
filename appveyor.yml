version: 1.0.{build}
image:
- Visual Studio 2017

platform:
  - x64

init:
  - git config --global core.autocrlf input

install:
  - ps: Install-Product node stable x64
  - npm install

build_script:
  - npx envinfo
  - npm run dist

test: off
