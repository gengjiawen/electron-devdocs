version: 1.0.{build}

platform:
  - x64

init:
  - git config --global core.autocrlf input

install:
  - ps: Install-Product node stable x64
  - npm install
  - npm prune

build_script:
  - npx envinfo
  - npm run dist

test: off
